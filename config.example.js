/* =========================================================
   Configuração do Firebase - FICHEIRO DE EXEMPLO
   ==========================================================
   Este é um modelo com valores de espaço reservado FALSOS.
   Copia este ficheiro para `config.js` e preenche com as tuas
   credenciais a partir da Firebase Console.

   Passos para obter as tuas credenciais:
   1. Acede a https://console.firebase.google.com/
   2. Cria ou abre o teu projeto
   3. Definições do Projeto (ícone de engrenagem) > Geral > As tuas apps
   4. Adiciona uma Web app (</>) se não tiveres uma
   5. Copia os valores firebaseConfig para este ficheiro
   6. Renomeia o ficheiro para `config.js`

   NOTA: `config.js` está no git-ignore e NUNCA será
   commitado. Nunca coloques credenciais reais neste ficheiro de exemplo.
   ========================================================= */

const firebaseConfig = {
  apiKey: "YOUR_API_KEY_HERE",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project",
  storageBucket: "your-project.firebasestorage.app",
  messagingSenderId: "000000000000",
  appId: "1:000000000000:web:0000000000000000000000",
  measurementId: "G-XXXXXXXXXX"
};
